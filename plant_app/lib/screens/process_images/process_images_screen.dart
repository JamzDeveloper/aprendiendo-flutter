import 'package:flutter/material.dart';

class ProcessImageScreen extends StatelessWidget {
  const ProcessImageScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
